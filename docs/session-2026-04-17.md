# Session report — 2026-04-17

Handoff doc for the next session. Everything below is on `origin/master`.

## What shipped (in commit order)

25 commits, grouped by theme:

### MM Confluence v2 scanner

- `e5fabff` feat(strategies): MM Confluence v2 setup scanner — pure math in `lib/scanner/mmConfluence.ts` (ema, rsi, detectMWFormation, scoreSetup, checkInvalidation, sessionInfo). API at `/api/scanner`. `SetupScanner` client component on `/strategies`. Crypto-only v1 (forex skipped with `forex-no-ohlc`).
- `240e0a7` · `5b7c469` docs: refresh CLAUDE.md, README, docs/architecture.md, TODO.md to match the real app (twice — once after scanner, once after reliability work).

### Reliability hardening

- `9eb3c54` add `app/error.tsx`, `app/not-found.tsx`, per-route `loading.tsx` skeletons; log silent catches in API routes.
- `4280f13` Zod-validate upstream responses in `/api/crypto/{prices,ohlc/[id]}` and `/api/forex/rates`; wire pino via `lib/logger.ts`.
- `06ef3c5` Zod-guard `useStrategies`/`useWatchlist` on load; add export/import JSON bundle buttons (tradehub bundle schema v1).

### Testing

- `7f07f54` Vitest + @testing-library/react. 37 unit tests covering `lib/patterns/detector.ts` (fixtures per pattern), `lib/scanner/mmConfluence.ts` (ema/rsi/M-W/score/invalidation/session), `lib/hooks/useMarketData.ts` transforms.
- `e6e3005` Playwright smoke E2E: every route returns 200, sidebar renders, no console errors. 6 tests green.

### CI/CD + deploy

- `c5d52b7` `.github/workflows/ci.yml` — lint / typecheck / vitest / next build / playwright chromium / gitleaks on every PR + push to master.
- `a155a3f` gitleaks secret scan job.
- `725d928` Vercel linked to `jcincnet11s-projects/trade-hub`. `make deploy TARGET=prod|preview` + `make logs` + `make status` wired to Vercel CLI. `<Analytics />` mounted.

### Observability

- `d65d1eb` `/api/health` returning `{ status, sha, env, at }`.

### Performance

- `2e030f0` memoize `detectPatterns(candles)` in the crypto DetailPanel; per-route metadata via `{route}/layout.tsx` (client pages can't export `metadata` directly). Title template `%s · Trade Hub`.

### UX polish

- `a219b82` skeleton cards on `/crypto` and `/forex` (replacing "Loading…" text); red error banners when SWR fails; confirm-before-delete on strategy cards.

### DX

- `1916478` Prettier + `eslint-config-prettier`. `.prettierrc.json`, `.prettierignore`, `make fmt` / `make fmt-check`.
- `44d281d` Husky + lint-staged. Pre-commit runs prettier on all text files and eslint --fix on TS.
- `f00893c` `docs/onboarding.md` — clone to running in under 10 minutes.

### Charts feature (the big one)

- `fcc5abc` variable `?days=N` on `/api/crypto/ohlc/[id]`; new `/api/forex/ohlc/[pair]` hitting Frankfurter with synthesized OHLC (daily ECB close ±0.1% wicks). `lib/data.ts` client fetchers. `detectAll(candles): PatternHit[]` added to the pattern engine (series-variant; the scanner still uses the original `detectPatterns`).
- `2b91c19` `components/market/Sparkline.tsx` (SVG polyline) and `components/market/CandleChart.tsx` (lightweight-charts v5 with `createSeriesMarkers` for pattern annotations).
- `d8e1a58` `/crypto/[id]` and `/forex/[pair]` detail pages — timeframe toggles, full candle chart, pattern list, "New strategy" deep-link to `/strategies?new=1&market=X&pair=Y`.
- `d8ef10f` `/crypto` and `/forex` list rows now link to detail pages via `router.push`; each row shows a 7-day (crypto) / 30-day (forex) sparkline. Inline DetailPanel removed (replaced by detail route).
- `e65f12f` **Strategies page rewrite** — `<SetupScanner />` preserved up top; form CRUD replaced with trade-plan model (numeric entry/stop/target, direction, status active/triggered/closed, pattern watch, auto-trigger every 2 min). New hook `lib/hooks/useTradePlans.ts`, new schema in `lib/schemas.ts`, new localStorage key `tradehub.strategies.v1`. Old key `trade-hub-strategies` **orphaned** (not migrated; retrievable via devtools).
- `e40ed12` extend timeframes: crypto adds 14D/180D/1Y, forex adds 7D/14D.
- `0e8b47c` **crypto intraday** — `/api/crypto/intraday/[id]?interval=15m|30m` buckets 5-min market_chart data into real OHLC. Detail page gains `15m` and `30m` buttons.
- `9cf1c40` **forex intraday** — `/api/forex/intraday/[pair]?interval=15min|30min|1h` via Twelve Data. 503 with clickable signup link if `TWELVE_DATA_API_KEY` is missing.

## Current state

- **Branch:** `master`, fully pushed to `origin/master`.
- **Working tree:** clean.
- **Typecheck / lint / tests / build:** all green at last run.
- **Latest preview:** `https://trade-lm9detatl-jcincnet11s-projects.vercel.app`
- **Production:** `trade-hub-steel-five.vercel.app` — **not yet redeployed** since the charts/strategies rewrite; run `make deploy TARGET=prod` when ready.

## Environment variables

| Name                  | Production | Preview | Development |
| --------------------- | :--------: | :-----: | :---------: |
| `TWELVE_DATA_API_KEY` |     ✅     |   ✅    |     ✅      |

`.env.local` also has the key for `npm run dev`. **The key is sensitive — don't commit `.env.local`** (it's gitignored, but worth noting).

## Open items

Only two things remain from the original backlog:

- **Backing-store decision** for strategies (Supabase Postgres is the default). Product call — not a coding task. Once decided, update `docs/architecture.md` and plan a migration from the `tradehub.strategies.v1` localStorage key.
- **Sentry** — server + client error tracking. Needs a DSN (free tier available at sentry.io). When you have the DSN, wire via `@sentry/nextjs`.

New items surfaced this session:

- Orphaned old strategy data lives at `localStorage["trade-hub-strategies"]`. Either (a) write a one-time migration into `tradehub.strategies.v1`, or (b) delete the key when confident no one needs it.
- `components/market/PriceChart.tsx` is now unused on list pages (DetailPanel was removed). Decide: keep as-is (harmless) or delete (cleanup).

## Routes inventory

Pages:

- `/` — dashboard (aggregated sentiment)
- `/crypto` — grid w/ sparklines → click → `/crypto/[id]` detail (timeframes 15m/30m/1D/7D/14D/30D/90D/180D/1Y)
- `/forex` — same shape → `/forex/[pair]` (timeframes 15m/30m/1h/7D/14D/30D/90D/180D/1Y)
- `/strategies` — scanner on top, trade-plan CRUD below, auto-triggers every 2 min
- `/watchlist` — user-saved pairs

API:

- `/api/crypto/prices` — CoinGecko /simple/price for 10 coins, `revalidate: 60`
- `/api/crypto/ohlc/[id]?days=N` — CoinGecko /ohlc, `revalidate: 300`
- `/api/crypto/intraday/[id]?interval=15m|30m` — CoinGecko /market_chart bucketed, `revalidate: 300`
- `/api/forex/rates` — ExchangeRate API latest, `revalidate: 3600`
- `/api/forex/ohlc/[pair]?days=N` — Frankfurter synthesized, `revalidate: 3600`
- `/api/forex/intraday/[pair]?interval=15min|30min|1h` — Twelve Data, `revalidate: 300`
- `/api/scanner?tf=30min|4H|1D&symbols=a,b,c` — MM Confluence v2 scoring
- `/api/health` — `{ status, sha, env, at }`

## Where things live (if picking up cold)

- `lib/patterns/` — 21 candlestick detectors + `detectPatterns` (last-candle, for scanner) + `detectAll` (series, for chart markers).
- `lib/scanner/mmConfluence.ts` — pure indicators + scoring.
- `lib/coingecko.ts` — server-side CoinGecko helpers (fetchOHLC, fetchVolumes).
- `lib/data.ts` — **client-side** helpers calling our own proxies (fetchCryptoOHLC, fetchCryptoIntraday, fetchForexOHLC, fetchForexIntraday).
- `lib/schemas.ts` — all Zod schemas (CoinGecko, Frankfurter, Twelve Data, strategies, trade plans, export bundle).
- `lib/logger.ts` — pino, pretty in dev, JSON in prod.
- `lib/hooks/` — SWR + localStorage hooks. Watchlist is Zod-guarded; strategies (old) and trade plans (new) both exist as separate hooks for now.
- `components/market/` — PairCard, PatternBadge, PriceChart (legacy, unused on lists), CandleChart (new), Sparkline.
- `app/strategies/_components/SetupScanner.tsx` — live MM Confluence cards.
- `docs/strategies/mm-confluence-v2.md` — strategy spec.

## Suggested next steps

1. Promote preview to prod (`make deploy TARGET=prod`) once you've clicked through the new chart UI and verified forex intraday renders.
2. Decide on the backing store; if yes, start with a Supabase schema that matches `tradePlanSchema` and migrate `tradehub.strategies.v1`.
3. Wire Sentry — DSN goes in Vercel env, then install `@sentry/nextjs` and follow the wizard.
4. Delete `components/market/PriceChart.tsx` once confident nothing imports it.
