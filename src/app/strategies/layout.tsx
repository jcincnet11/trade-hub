import type { Metadata } from 'next'

export const metadata: Metadata = {
  title: 'Strategies',
  description: 'MM Confluence v2 live scanner + your personal trading playbook.',
}

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
